var assert = require('assert');
var should = require('should');

var utils = require('../lib/utils');
var App = require('../');

describe('install plugin', function () {
  it('should install specified dependency:', function () {

  });
});
